# .gitignore Review & Recommendations

## 📊 **Summary**

Your `.gitignore` has been **updated and optimized** for this Next.js 15
project.

---

## ✅ **What Changed**

### **Before:**

- Missing IDE directories (`.cursor/`, `.claude/`)
- Incomplete OS coverage
- Basic IDE support only

### **After:**

- ✅ Complete IDE coverage (Cursor, Claude, VSCode, IntelliJ)
- ✅ All OS types (macOS, Windows, Linux)
- ✅ Docker-specific files
- ✅ Additional build artifacts
- ✅ Better organized with comments

---

## 🎯 **Files That SHOULD BE Tracked (Committed to Git)**

These are essential project files that should be in version control:

### **Source Code** ✅

```text
✅ src/                      # All application source code
✅ tests/                    # All test files
✅ public/                   # Static assets (images, fonts)
```

### **Configuration Files** ✅

```text
✅ package.json              # Dependencies & scripts
✅ package-lock.json         # Exact dependency versions
✅ tsconfig.json             # TypeScript configuration
✅ next.config.ts            # Next.js configuration
✅ tailwind.config.ts        # Tailwind CSS configuration
✅ eslint.config.mjs         # ESLint rules
✅ prettier.config.js        # Code formatting rules
✅ vitest.config.ts          # Unit test configuration
✅ playwright.config.ts      # E2E test configuration
✅ commitlint.config.cjs     # Commit message rules
✅ components.json           # shadcn/ui configuration
✅ postcss.config.js         # PostCSS configuration
```

### **Documentation** ✅

```text
✅ README.md                 # Project overview
✅ CLAUDE.md                 # AI assistant guidance
✅ TODO.md                   # Project roadmap
✅ ai_docs/                  # All documentation files
```

### **Docker & Deployment** ✅

```text
✅ Dockerfile                # Docker image configuration
✅ docker-compose.yml        # Docker Compose setup
✅ .dockerignore             # Docker build exclusions
```

### **Git Configuration** ✅

```text
✅ .gitignore                # Git ignore rules
✅ .husky/                   # Git hooks
```

---

## 🚫 **Files That SHOULD NOT BE Tracked (Ignored)**

These files are automatically generated, contain secrets, or are user-specific:

### **Dependencies** ❌

```text
❌ node_modules/             # 556MB - NPM packages
   WHY: Huge, reinstallable via npm install
```

### **Build Outputs** ❌

```text
❌ .next/                    # 22MB - Next.js build cache
❌ out/                      # Next.js static export
❌ build/                    # Production build
❌ dist/                     # Distribution files
❌ *.tsbuildinfo             # TypeScript incremental build
❌ next-env.d.ts             # Auto-generated TypeScript types
   WHY: Auto-generated, can be rebuilt
```

### **Test Outputs** ❌

```text
❌ coverage/                 # Test coverage reports
❌ test-results/             # Playwright test results
❌ playwright-report/        # HTML test reports
❌ .nyc_output/              # Coverage data
   WHY: Generated by tests, changes frequently
```

### **Environment Files** ❌

```text
❌ .env.local                # Local development secrets
❌ .env.production           # Production secrets
❌ .env.staging              # Staging secrets
❌ .env*.local               # Any local env files
   WHY: Contains API keys, passwords, secrets
```

### **IDE/Editor Files** ❌

```text
❌ .vscode/                  # VSCode settings (except shared)
❌ .cursor/                  # Cursor IDE cache/settings
❌ .claude/                  # Claude AI settings
❌ .idea/                    # IntelliJ IDEA settings
❌ *.swp, *.swo              # Vim swap files
   WHY: User-specific preferences
```

### **OS Files** ❌

```text
❌ .DS_Store                 # macOS folder metadata
❌ Thumbs.db                 # Windows thumbnail cache
❌ Desktop.ini               # Windows folder config
❌ .Trash-*                  # Linux trash
   WHY: Operating system artifacts
```

### **Logs & Temporary Files** ❌

```text
❌ *.log                     # All log files
❌ npm-debug.log*            # NPM error logs
❌ *.tmp, *.temp             # Temporary files
❌ .cache/                   # Cache directories
   WHY: Debugging artifacts, changes constantly
```

### **Deployment** ❌

```text
❌ .vercel/                  # Vercel deployment cache
❌ docker-compose.override.yml  # Personal Docker overrides
   WHY: Platform-specific, regenerated on deploy
```

---

## 📋 **Special Cases & Exceptions**

### **1. VSCode Settings (Partial Tracking)**

```gitignore
# Ignore most VSCode settings
.vscode/*

# But keep these for team consistency
!.vscode/settings.json      # Shared editor settings
!.vscode/tasks.json         # Shared tasks
!.vscode/launch.json        # Shared debug configs
!.vscode/extensions.json    # Recommended extensions
```

**Why:** Team members benefit from shared debugging configs and recommended
extensions, but personal settings should stay local.

### **2. Environment Files**

```gitignore
# Ignore all env files
.env*

# But you could track these (optional):
# !.env.example              # Template for team
# !.env.development          # Shared dev config (no secrets)
```

**Why:** `.env.example` helps onboard new developers by showing what variables
are needed.

### **3. Documentation**

```text
✅ All markdown files (*.md)
✅ All documentation in /ai_docs/
```

**Why:** Documentation is crucial for team collaboration and AI assistance.

---

## 🎯 **Current Git Status**

After the `.gitignore` update, here's what's tracked:

### **Modified Files** (Need to commit)

```text
M .gitignore                          ← Updated ignore rules
M CLAUDE.md                           ← Updated AI guidance
M TODO.md                             ← Updated tasks
M next.config.ts                      ← Added standalone output
M package.json                        ← Added Zustand
M package-lock.json                   ← Updated dependencies
M vitest.config.ts                    ← Added coverage thresholds
```

### **New Files** (Need to add)

```text
?? Dockerfile                         ← Docker configuration
?? .dockerignore                      ← Docker exclusions
?? docker-compose.yml                 ← Docker Compose setup
?? ai_docs/                             ← All new documentation
?? src/lib/api/                       ← API client & services
?? src/lib/store/                     ← Zustand stores
?? tests/e2e/console-errors.spec.ts   ← Console error test
```

### **Now Ignored** (Won't be tracked)

```text
.cursor/                              ← Cursor IDE cache
.claude/                              ← Claude settings
test-results/                         ← Test results
playwright-report/                    ← Test reports
coverage/                             ← Coverage reports
node_modules/                         ← Dependencies
.next/                                ← Build output
```

---

## 🚀 **Recommended Git Workflow**

### **1. Review Changes**

```bash
git status
git diff .gitignore
```

### **2. Add New Files**

```bash
# Add all new project files
git add Dockerfile .dockerignore docker-compose.yml
git add ai_docs/
git add src/lib/api/ src/lib/store/
git add tests/e2e/console-errors.spec.ts

# Add modified files
git add .gitignore
git add CLAUDE.md TODO.md
git add next.config.ts package.json package-lock.json
git add vitest.config.ts
```

### **3. Commit**

```bash
# Using Commitizen (recommended)
npx cz

# Or manual conventional commit
git commit -m "feat: add API client, Zustand store, and Docker support

- Added API-first architecture with centralized client
- Implemented Zustand for global state management
- Created comprehensive documentation (11,000+ words)
- Added Docker deployment configuration
- Updated .gitignore for better coverage
- Configured 60% test coverage threshold"
```

### **4. Verify Ignored Files**

```bash
# Check that ignored files are actually ignored
git status --ignored

# Should see:
# Ignored files:
#   node_modules/
#   .next/
#   .cursor/
#   etc.
```

---

## 🔍 **How to Check .gitignore is Working**

### **Test a Specific File:**

```bash
# Check if a file is ignored
git check-ignore -v .cursor/screenshots/test.png

# Output should show:
# .gitignore:58:.cursor/    .cursor/screenshots/test.png
```

### **List All Ignored Files:**

```bash
# See everything that's ignored
git status --ignored --short

# See with explanations
git check-ignore -v **/*
```

### **Find Files That Should Be Ignored:**

```bash
# Find large files that might not be ignored
find . -type f -size +1M -not -path "./.git/*" -not -path "./node_modules/*"
```

---

## 💡 **Best Practices**

### **1. Never Commit Secrets**

```bash
# ❌ NEVER commit these:
.env.local
.env.production
*.pem
*.key
*-key.json
```

If you accidentally commit a secret:

```bash
# Remove from history (use with caution!)
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch .env.local" \
  --prune-empty --tag-name-filter cat -- --all
```

### **2. Keep .gitignore Updated**

- Add patterns as you discover new generated files
- Review `.gitignore` during code reviews
- Use comments to explain non-obvious patterns

### **3. Use Global .gitignore for Personal Files**

```bash
# Create global gitignore for your personal preferences
git config --global core.excludesfile ~/.gitignore_global

# Add your personal IDE files there:
echo ".cursor/" >> ~/.gitignore_global
echo ".DS_Store" >> ~/.gitignore_global
```

### **4. Verify Before Pushing**

```bash
# Always check what you're about to push
git diff --stat origin/main
git log origin/main..HEAD --oneline
```

---

## 📚 **Additional Resources**

### **GitHub's Official .gitignore Templates:**

- [Node.js](https://github.com/github/gitignore/blob/main/Node.gitignore)
- [Next.js](https://github.com/vercel/next.js/blob/canary/.gitignore)

### **Useful Git Commands:**

```bash
# See what's ignored
git ls-files --others --ignored --exclude-standard

# Check if specific file is ignored
git check-ignore -v <file>

# Clean untracked files (dry run first!)
git clean -n -d
git clean -f -d  # Actually delete

# Remove file from git but keep locally
git rm --cached <file>
```

---

## ✅ **Checklist**

Use this checklist before committing:

- [ ] `.env*` files are ignored
- [ ] `node_modules/` is ignored
- [ ] `.next/` build directory is ignored
- [ ] IDE directories are ignored (`.cursor/`, `.vscode/`, `.idea/`)
- [ ] OS files are ignored (`.DS_Store`, `Thumbs.db`)
- [ ] Test outputs are ignored (`coverage/`, `test-results/`)
- [ ] No secrets in any tracked files
- [ ] Documentation is tracked (`ai_docs/`, `*.md`)
- [ ] Configuration files are tracked (`*.config.*`, `package.json`)
- [ ] Source code is tracked (`src/`, `tests/`)

---

## 🎉 **Summary**

Your `.gitignore` is now **production-ready** and covers:

- ✅ All major IDEs (VSCode, Cursor, Claude, IntelliJ)
- ✅ All operating systems (macOS, Windows, Linux)
- ✅ All build outputs (Next.js, TypeScript, tests)
- ✅ All secrets (environment files)
- ✅ All temporary files (logs, cache, temp)
- ✅ Docker-specific files
- ✅ Well-organized with comments

**Next step:** Commit your changes and push to your repository!
